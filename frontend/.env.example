# Copy this file to .env and set your values.
# Vite will expose variables prefixed with VITE_ to the frontend.

# Backend API URL — change for staging/production deployments
VITE_API_URL=http://127.0.0.1:8000
